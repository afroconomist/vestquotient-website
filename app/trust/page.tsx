import { Check, LockKeyhole, Scale, ShieldCheck } from 'lucide-react'
import { Header } from '@/components/Header'
import { Footer } from '@/components/Footer'
import { PageHero } from '@/components/PageHero'
import { CTABand } from '@/components/CTABand'

const principles = [['Protection', 'Client funds and securities are segregated, reconciled and handled within approved controls.'], ['Accountability', 'Independent compliance oversight reports directly to the Board and its committees.'], ['Transparency', 'Clients receive clear confirmations, portfolio valuations, charges and trade reporting.'], ['Resolution', 'Questions and complaints are acknowledged, investigated and resolved through a documented escalation process.']]
export default function TrustPage() { return <main className="site-shell"><Header /><PageHero title={<>Confidence is<br /><em>structured.</em></>} subtitle="Trust & governance" copy="We believe the strongest client relationships are built on controls that are visible, consistent and independently challenged." /><section className="section container"><div className="principle-grid">{principles.map(([title, copy], i) => <article className="principle-card" key={title}><div className="principle-icon">{i === 0 ? <LockKeyhole /> : i === 1 ? <Scale /> : i === 2 ? <ShieldCheck /> : <Check />}</div><span>0{i + 1}</span><h2>{title}</h2><p>{copy}</p></article>)}</div></section><section className="section section-tint"><div className="container split-copy"><div><div className="section-label">02 <span>Client charter</span></div><h2>What you can expect from us.</h2></div><div className="check-list"><div><Check />Fair and transparent pricing</div><div><Check />Professional, respectful communication</div><div><Check />Confidential handling of client information</div><div><Check />Timely trade confirmations and statements</div><div><Check />A clear complaint and escalation pathway</div><div><Check />Annual independent audit of financial controls</div></div></div></section><CTABand title="Have a question about our controls?" buttonText="Contact compliance" buttonHref="mailto:info@vestquotientcapital.com" /><Footer /></main> }
